import * as _colors from "./temp.js"

const colors = {
	transparent: "#fff0",
	black: "#000",
	white: "#fff",
	..._colors,
	base: _colors.slate,
	hipen: _colors.gold,
	prime: _colors.blue,
}

export default colors
